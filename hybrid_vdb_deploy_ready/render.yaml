services:
  - type: web
    name: hybrid-vdb
    env: python
    plan: free
    rootDir: src
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn demo.app:app --host 0.0.0.0 --port $PORT"
